{
  "name": "namastenest",
  "version": "1.0.0",
  "description": "Namaste Nest — Daily Rental Guest House, Mysore",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "seed": "node server/seed-standalone.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
